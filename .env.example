# ===========================================
# Backend Configuration (后端配置)
# ===========================================

# DashScope API Key (必填)
DASHSCOPE_API_KEY=your_dashscope_api_key_here

# Text Generation Model (default: deepseek-v3.2)
DASHSCOPE_MODEL=deepseek-v3.2

# Vision-Language Model for multimodal input (default: qwen3-vl-plus)
# Options: qwen3-vl-plus (supports thinking), qwen3-vl-flash (lightweight), qwen-vl-plus (legacy)
DASHSCOPE_VL_MODEL=qwen3-vl-plus

# Enable thinking/reasoning output (default: true, recommended for better PRD quality)
ENABLE_THINKING=true

# Debug mode - show detailed error messages (default: false)
DEBUG_ERRORS=false
cla
# ===========================================
# Port Configuration (端口配置)
# ===========================================
#
# Local Development (本地开发):
#   - Backend:  http://localhost:28000
#   - Frontend: http://localhost:3000
#   - Frontend .env.local: BACKEND_URL=http://localhost:28000
#
# Docker Compose (Docker 部署):
#   - Backend:  http://localhost:28000 (host) → :8000 (container)
#   - Frontend: http://localhost:23456 (host) → :3000 (container)
#   - docker-compose.yml 自动配置容器间网络
